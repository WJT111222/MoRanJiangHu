# 生图与图库系统移植实施计划

> 从 Rebase 分支移植到 Images 分支（基于 origin/main）
> 分析日期：2026-07-09

---

## 四准则审查

| 准则 | 说明 |
|------|------|
| **完整** | 覆盖生图代理、图片本地化容错、物品生图提示词、用户图库、立绘URL 5 大模块 |
| **通用** | 通过开关和自动判断兼容新旧环境，不影响原有功能 |
| **明确** | 每个步骤含完整代码片段、精确行号、冲突评估 |
| **无副作用** | 所有改动增量，main 原有逻辑不变 |

---

## 移植顺序（按依赖拓扑）

```
第1步：数据模型（无依赖）
  models/system.ts / character.ts / social.ts

第2步：工具函数（依赖数据模型）
  utils/personAvatar.ts / utils/imageAssets.ts

第3步：配置层（依赖工具函数）
  utils/apiConfig.ts

第4步：生图核心（依赖配置层）
  services/ai/imageGenerationDiagnostics.ts
  services/ai/imageTasks.ts（仅生图代理+本地化容错部分）
  services/ai/openaiImageTest.ts

第5步：物品生图（依赖生图核心）
  services/ai/itemImageGeneration.ts

第6步：用户图库（依赖生图核心+dbService）
  services/dbService.ts
  utils/itemImage.ts

第7步：Vite 代理（独立）
  vite.config.ts

第8步：UI 层（依赖以上全部）
  components/features/Settings/ImageGenerationSettings.tsx
  components/features/Social/ImageManagerModal.tsx
  components/features/Inventory/InventoryModal.tsx
  各立绘显示组件
```

---

## 第1步：数据模型

### 1.1 `models/character.ts`

在 `角色数据结构` 接口中新增：

```typescript
export interface 角色数据结构 {
    姓名: string;
    头像图片URL?: string;
    立绘图片URL?: string;  // ← 新增
    图片档案?: NPC图片档案;
    // ... 其余不变
}
```

**冲突**：✅ 无

### 1.2 `models/social.ts`

在 `NPC结构` 末尾新增：

```typescript
export interface NPC结构 {
    // ... 原有字段
    头像图片URL?: string;
    立绘图片URL?: string;  // ← 新增
}
```

**冲突**：✅ 无

### 1.3 `models/system.ts`

在 `初始伙伴配置结构` 中新增：

```typescript
export interface 初始伙伴配置结构 {
    enabled: boolean;
    头像图片URL?: string;
    立绘图片URL?: string;  // ← 新增
    图片档案?: import('./imageGeneration').NPC图片档案;
    // ... 其余不变
}
```

**冲突**：✅ 无

---

## 第2步：工具函数

### 2.1 `utils/personAvatar.ts`

新增 `提取人物立绘地址` 函数：

```typescript
export const 提取人物立绘地址 = (person: any): string => {
    if (person?.立绘图片URL) return 获取图片资源文本地址(person.立绘图片URL);
    const archive = person?.图片档案 && typeof person.图片档案 === 'object' ? person.图片档案 : {};
    const history = Array.isArray(archive?.生图历史) ? archive.生图历史 : [];
    const selectedPortraitId = typeof archive?.已选立绘图片ID === 'string' ? archive.已选立绘图片ID.trim() : '';
    const selected = selectedPortraitId ? history.find((item) => item?.id === selectedPortraitId) : null;
    if (获取图片展示地址(selected)) return 获取图片展示地址(selected);
    const portraitRecord = history.find((item) => item?.状态 === 'success' && (item?.构图 === '立绘' || item?.构图 === '半身') && 获取图片展示地址(item));
    return 获取图片展示地址(portraitRecord) || '';
};
```

**冲突**：✅ 无

### 2.2 `utils/imageAssets.ts`

新增 `解析远程图片为可显示地址` 函数：

```typescript
const 是否本机或局域网地址 = (url: string): boolean => {
    try {
        const u = new URL(url);
        const h = u.hostname.toLowerCase();
        if (h === 'localhost' || h === '127.0.0.1' || /^\[?::1\]?$/.test(h) || h === '0.0.0.0') return true;
        if (/^127\./.test(h) || /^10\./.test(h) || /^192\.168\./.test(h)) return true;
        if (/^172\.(1[6-9]|2\d|3[0-1])\./.test(h)) return true;
        if (/^169\.254\./.test(h) || /\.local$/i.test(h)) return true;
        return false;
    } catch { return false; }
};

const 解析远程图片为可显示地址 = (url: string): string => {
    if (!/^https:\/\//i.test(url)) return url;
    if (是否本机或局域网地址(url)) return url;
    if (typeof window !== 'undefined' && !isNativeCapacitorEnvironment()) {
        const base = buildSyncApiUrl('/api/image-backend/fetch-image');
        const separator = base.includes('?') ? '&' : '?';
        return `${base}${separator}url=${encodeURIComponent(url)}`;
    }
    return url;
};
```

**冲突**：✅ 无

---

## 第3步：配置层

### 3.1 `utils/apiConfig.ts`

`当前可用接口结构` 新增两个字段：

```typescript
export type 当前可用接口结构 = Pick<单接口配置结构, '...'> & {
    // ... 原有字段
    图片需要代理?: boolean;        // ← 新增
    自定义图片代理地址?: string;    // ← 新增
};
```

`获取文生图接口配置` 中新增透传：

```typescript
export const 获取文生图接口配置 = (settings: 接口设置结构): 当前可用接口结构 | null => {
    // ... 原有逻辑
    return {
        // ... 原有字段
        图片需要代理: Boolean(feature?.图片需要代理),                    // ← 新增
        自定义图片代理地址: typeof feature?.自定义图片代理地址 === 'string'
            ? feature.自定义图片代理地址.trim() : '',  // ← 新增
    };
};
```

`获取NSFW文生图接口配置` 中新增：

```typescript
const nsfw自定义代理地址 = 读取字符串(feature?.NSFW自定义图片代理地址).trim();
// result 中：
自定义图片代理地址: nsfw自定义代理地址 || (canReuseSharedConnection ? (sharedConfig?.自定义图片代理地址 || '') : ''),
```

**冲突**：⚠️ 低。MCX 移植时已经加了 `自定义图片代理地址` 字段，这里再加 `图片需要代理` 不会冲突。

---

## 第4步：生图核心

### 4.1 `services/ai/imageGenerationDiagnostics.ts`

**核心改动：代理决策系统**

```typescript
// 新增导入
import { requiresRemoteSyncApi } from '../../utils/nativeRuntime';

// 新增常量
const 预定义供应商目标映射: Record<string, string> = {
    'openai-official': 'https://api.openai.com'
};

// 新增函数
const 当前是在线环境 = (): boolean => {
    if (typeof window === 'undefined') return false;
    const origin = window.location.origin;
    return origin === 'https://msjh.bacon159.pp.ua' || origin === 'https://msjh.bacon.de5.net';
};

const 可走同域图片代理 = (): boolean => {
    if (typeof window === 'undefined') return false;
    if (isNativeCapacitorEnvironment()) return requiresRemoteSyncApi();
    return /^https?:$/i.test(window.location.protocol);
};

const 是否本地开发环境 = (): boolean => {
    if (typeof window === 'undefined') return false;
    const h = window.location.hostname.toLowerCase();
    return h === 'localhost' || h === '127.0.0.1' || h === '[::1]' || h === '0.0.0.0';
};

// 代理决策类型
type 代理决策 = {
    走代理: boolean;
    原因: string;
    原始baseUrl: string;
    供应商ID: string;
    origin: string;
    isNative: boolean;
};

// 核心决策函数
export const 获取代理决策 = (
    baseUrlRaw: string,
    供应商ID: string,
    options?: { useRuntimeProxy?: boolean; 自定义图片代理地址?: string }
): 代理决策 => {
    const origin = typeof window !== 'undefined' ? window.location.origin : 'unknown';
    const isNative = isNativeCapacitorEnvironment();
    const 本地开发 = 是否本地开发环境();
    const 在线 = 当前是在线环境();
    const 有自定义代理 = !!(options?.自定义图片代理地址 || '').trim();

    if (有自定义代理 && options?.useRuntimeProxy) {
        return { 走代理: true, 原因: '用户自定义代理地址', 原始baseUrl: baseUrlRaw, 供应商ID, origin, isNative };
    }
    if (本地开发 && options?.useRuntimeProxy && 可走同域图片代理()) {
        return { 走代理: true, 原因: '本地开发环境自动走代理', 原始baseUrl: baseUrlRaw, 供应商ID, origin, isNative };
    }
    if (options?.useRuntimeProxy && 在线 && 预定义供应商目标映射[供应商ID]) {
        return { 走代理: true, 原因: '在线环境+官方白名单', 原始baseUrl: baseUrlRaw, 供应商ID, origin, isNative };
    }
    return { 走代理: false, 原因: 本地开发 ? '本地开发但代理不可用' : '在线环境直连', 原始baseUrl: baseUrlRaw, 供应商ID, origin, isNative };
};

// 重写端点构建
export const 构建OpenAI图片生成端点 = (
    baseUrlRaw: string,
    customPathRaw?: string,
    options?: { useRuntimeProxy?: boolean; 供应商ID?: string; 自定义图片代理地址?: string }
): string => {
    const 供应商ID = options?.供应商ID || 'openai-custom';
    const 决策 = 获取代理决策(baseUrlRaw, 供应商ID, options);

    if (决策.走代理) {
        const proxyBase = 获取运行时代理基础地址(options?.自定义图片代理地址);
        if (预定义供应商目标映射[供应商ID]) {
            const path = customPathRaw || '/v1/images/generations';
            return `${proxyBase}/api/image-backend/openai-image-proxy${path}?provider=${encodeURIComponent(供应商ID)}`;
        }
        const targetUrl = `${规范化OpenAI图片基础地址(baseUrlRaw).replace(/\/+$/, '')}${customPathRaw && customPathRaw.startsWith('/') ? customPathRaw : `/${customPathRaw || '/v1/images/generations'}`}`;
        return `${proxyBase}/api/image-backend/openai-image-proxy?url=${encodeURIComponent(targetUrl)}`;
    }
    return 构建直连端点(baseUrlRaw, customPathRaw);
};

// 修改获取运行时代理基础地址签名
const 获取运行时代理基础地址 = (自定义代理地址?: string): string => {
    const 用户自定义 = (自定义代理地址 || '').trim();
    if (用户自定义) return 用户自定义.replace(/\/+$/, '');
    if (typeof window !== 'undefined' && /^https?:$/i.test(window.location.protocol) && !isNativeCapacitorEnvironment()) {
        return window.location.origin.replace(/\/+$/, '');
    }
    return (RELEASE_INFO.websiteUrl || 'https://msjh.bacon.de5.net').replace(/\/+$/, '');
};
```

**冲突**：⚠️ 中。MCX 已经修改了 `获取运行时代理基础地址` 的 import（加了 `requiresRemoteSyncApi`），这里需要确认不重复。

### 4.2 `services/ai/imageTasks.ts`

**仅移植生图代理+本地化容错部分**

#### 4.2.1 新增导入

```typescript
import { 注册远程图片兜底引用, 创建图片资源引用 } from '../../utils/imageAssets';
import { recordDiagnosticLog } from '../../services/diagnosticLog';
import { 获取代理决策 } from './imageGenerationDiagnostics';
```

#### 4.2.2 修改 `构建图片端点`

```typescript
const 构建图片端点 = (baseUrlRaw: string, customPathRaw?: string, 供应商ID?: string, 自定义图片代理地址?: string, 图片需要代理?: boolean): string => {
    // ... NovelAI 路径不变
    return 构建OpenAI图片生成端点(base, customPath, { useRuntimeProxy: true, 供应商ID, 自定义图片代理地址, 图片需要代理 });
};
```

#### 4.2.3 修改 `generateImageByPrompt`

```typescript
// 在 generateImageByPrompt 中：
const 供应商ID = apiConfig.图片后端类型 === 'openai' && /api\.openai\.com/i.test(apiConfig.baseUrl || '')
    ? 'openai-official'
    : (apiConfig.图片后端类型 || 'openai-custom');
const endpoint = 构建图片端点(apiConfig.baseUrl, apiConfig.图片接口路径, 供应商ID, apiConfig.自定义图片代理地址, apiConfig.图片需要代理);

// 日志
const 代理决策 = 获取代理决策(apiConfig.baseUrl, 供应商ID);
recordDiagnosticLog('info', '[生图请求] 准备发起', { ... });

// fetch 失败日志
recordDiagnosticLog('error', '[生图请求] fetch 失败', { ... });

// HTTP 错误日志
recordDiagnosticLog('error', '[生图请求] HTTP 错误响应', { ... });
```

#### 4.2.4 新增 `保存图片资源或降级`

```typescript
const 保存图片资源或降级 = async (dataUrl: string): Promise<string> => {
    try {
        return await dbService.保存图片资源(dataUrl);
    } catch (error: any) {
        recordDiagnosticLog('warn', '图片本地保存失败，降级为内联存储', { error: error?.message || String(error) });
        return dataUrl;
    }
};
```

#### 4.2.5 修改 `persistImageAssetLocally`

关键改动：所有 `dbService.保存图片资源` 调用替换为 `保存图片资源或降级`。

HTTP 下载路径重构（直连→代理 fallback + 远程兜底引用）：

```typescript
// 下载阶段
let response: Response | null = null;
let networkError: any = null;
let 尝试路径: string[] = [];
try {
    const firstUrl = shouldUseTemporaryImageProxy ? proxyUrl : imageUrl;
    尝试路径.push(firstUrl);
    response = await fetch(firstUrl);
} catch (error: any) {
    if (!shouldUseTemporaryImageProxy && /^https:\/\//i.test(imageUrl)) {
        尝试路径.push(proxyUrl);
        response = await fetch(proxyUrl);
    } else {
        throw error;
    }
} catch (error: any) {
    networkError = error;
}

// 网络不可达时降级为远程兜底引用
if (networkError) {
    if (/^https:\/\//i.test(imageUrl)) {
        const fallbackId = `img_remote_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`;
        注册远程图片兜底引用(imageUrl, fallbackId);
        recordDiagnosticLog('warn', '图片本地化失败，已登记远程兜底引用', { ... });
        return { ...compactResult, 图片URL: undefined, 本地路径: 创建图片资源引用(fallbackId) };
    }
    throw networkError;
}
```

**冲突**：⚠️ 中。MCX 已经修改了 `persistImageAssetLocally` 的部分内容，需要确认不重复。

### 4.3 `services/ai/openaiImageTest.ts`

新增 `探测图片Url取回代理`：

```typescript
export const 探测图片Url取回代理 = async (): Promise<string> => {
    try {
        const probe = `${buildSyncApiUrl('/api/image-backend/fetch-image')}?url=${encodeURIComponent('https://example.com/__moran_probe__.png')}`;
        const res = await fetch(probe, { method: 'GET' });
        return `url 取回代理可达（HTTP ${res.status}）...`;
    } catch (error: any) {
        return `url 取回代理不可达：${error?.message || error}...`;
    }
};
```

修改 `测试OpenAI兼容图片接口` 签名和调用。

**冲突**：✅ 无

---

## 第5步：物品生图

### 5.1 `services/ai/itemImageGeneration.ts`

#### 5.1.1 isChineseSupported 判断

```typescript
const isChineseSupported = /grok|imagine|gpt|image/i.test(imageApi.model || '');
```

#### 5.1.2 中文 Prompt 构建

```typescript
const prompt = isChineseSupported
    ? (() => {
        const 结构化描述 = structuredItem?.生图描述 || '';
        const 原始描述 = 读取文本(enrichedItem?.描述) || '';
        const 视觉描述 = 读取文本(enrichedItem?.视觉描述) || '';
        const 主体描述 = 结构化描述 || 视觉描述 || 原始描述 || `${读取文本(enrichedItem?.名称)}，${读取文本(enrichedItem?.类型)}`;
        const 画风约束: Record<string, string> = {
            '写实': '写实风格', '卡通': '卡通风格', '水墨': '水墨画风格',
            '像素': '像素风格', '油画': '油画风格', '素描': '素描风格',
            '国风': '中国风', '赛博朋克': '赛博朋克风格',
        };
        const 画风 = 画风约束[style] || style || '写实风格';
        return `${主体描述}，${画风}，简约背景，物品居中展示`;
    })()
    : 构建物品图提示词(enrichedItem, { 画风: style, 渲染风格: renderStyle, 来源位置: sourceLocation });
```

#### 5.1.3 附加正向/负面提示词

```typescript
const 附加正向 = isChineseSupported
    ? ''
    : `${enrichedItemIsLivingMount ? '...' : '...'}; ${noTextGuard}`;

const 附加负面提示词 = isChineseSupported
    ? ''
    : 构建物品负面提示词(enrichedItem);

const rawResult = await generateImageByPrompt(finalPrompt, imageApi, options?.signal, {
    构图: '物品图标',
    尺寸: size,
    附加正向提示词: 附加正向,
    附加负面提示词: 附加负面提示词,
    跳过基础负面提示词: isChineseSupported ? true : undefined,
});
```

#### 5.1.4 图库复用拦截

```typescript
// 在生成物品图标函数最前面
if (!options?.force && options?.题材模式 && options?.创意工坊模块ID) {
    const itemName = 提取图库物品名称(item);
    if (itemName) {
        try {
            const galleryEntry = await 查询用户图库图片(item, options.题材模式, options.创意工坊模块ID);
            if (galleryEntry?.assetId || galleryEntry?.imageUrl) {
                // 验证 assetId 有效性
                if (galleryEntry.assetId) {
                    try {
                        const exists = await 读取图片资源(galleryEntry.assetId);
                        if (exists) {
                            return { nextItem: item, imageRecord: { ... }, prompt: '', imageApi };
                        }
                    } catch {
                        await 删除用户图库条目(galleryEntry.id).catch(() => {});
                    }
                } else if (galleryEntry.imageUrl) {
                    return { nextItem: item, imageRecord: { ... }, prompt: '', imageApi };
                }
            }
        } catch { /* 查询失败继续生图 */ }
    }
}
```

**冲突**：⚠️ 中。main 的 `itemImageGeneration.ts` 有大量其他改动。

---

## 第6步：用户图库

### 6.1 `services/dbService.ts`

#### 6.1.1 新增常量和接口

```typescript
export const USER_IMAGE_GALLERY_STORE = 'user_image_gallery';

export interface 用户图库条目 {
    id: string;
    itemName: string;
    题材模式: string;
    创意工坊模块ID: string;
    assetId?: string;
    thumbnailDataUrl?: string;
    imageUrl?: string;
    prompt?: string;
    createdAt: number;
}
```

#### 6.1.2 数据库版本升级

```typescript
const VERSION = 4;  // 从 3 升级

// onupgradeneeded 中新增：
if (!db.objectStoreNames.contains(USER_IMAGE_GALLERY_STORE)) {
    const store = db.createObjectStore(USER_IMAGE_GALLERY_STORE, { keyPath: 'id' });
    store.createIndex('by_item_mode_workshop', ['itemName', '题材模式', '创意工坊模块ID'], { unique: true });
    store.createIndex('by_mode', '题材模式', { unique: false });
}
```

#### 6.1.3 新增 CRUD 函数

```typescript
export const 保存到用户图库 = async (entry: 用户图库条目): Promise<void> => { ... };
export const 查询用户图库图片 = async (itemName: string, 题材模式: string, 创意工坊模块ID: string): Promise<用户图库条目 | null> => { ... };
export const 获取用户图库全部条目 = async (): Promise<用户图库条目[]> => { ... };
export const 按题材模式获取用户图库 = async (题材模式: string): Promise<用户图库条目[]> => { ... };
export const 删除用户图库条目 = async (id: string): Promise<void> => { ... };
```

**冲突**：⚠️ 中。MCX 已经修改了 `dbService.ts`（图床上传自愈、配额超限等），VERSION 可能已经是 4。

### 6.2 `utils/itemImage.ts`

新增查询图库工具函数（约 100 行）。

**冲突**：✅ 无

---

## 第7步：Vite 代理

### 7.1 `vite.config.ts`

#### 7.1.1 修改 `handleOpenAiImageProxyRequest`

支持 `?url=` 模式（跳过路径白名单校验）：

```typescript
const handleOpenAiImageProxyRequest = async (req, res, next, logger) => {
    // ... OPTIONS 处理不变

    const requestUrl = new URL(req.url, 'http://local-openai-image-proxy');
    const targetBase = String(requestUrl.searchParams.get('url') || '').trim().replace(/\/+$/, '');
    const isUrlMode = !!targetBase;

    if (!isUrlMode) {
        // ?provider= 模式：路径白名单校验（原有逻辑）
        // ...
    }

    // 安全校验
    if (!targetBase || !isAllowedOpenAiImageProxyTarget(targetBase)) {
        res.statusCode = 400;
        res.end(JSON.stringify({ error: 'OpenAI image proxy target URL is invalid or not allowed.' }));
        return;
    }

    const target = new URL(targetBase);
    const basePath = target.pathname.replace(/\/+$/, '');

    if (isUrlMode) {
        // ?url= 模式：直接使用目标地址的完整路径
        target.pathname = basePath;
    } else {
        // ?provider= 模式：路径拼接
        const pathPart = requestUrl.pathname.startsWith(proxyPrefix)
            ? requestUrl.pathname.slice(proxyPrefix.length) || '/'
            : requestUrl.pathname || '/';
        const normalizedPath = basePath.endsWith('/v1') && pathPart.startsWith('/v1/')
            ? pathPart.replace(/^\/v1/i, '')
            : (pathPart.startsWith('/v1/') ? pathPart : `${basePath.endsWith('/v1') ? '' : '/v1'}${pathPart}`);
        target.pathname = `${basePath}${normalizedPath}`;
    }
    // ... 转发逻辑
};
```

#### 7.1.2 新增 `handleFetchImageProxyRequest`

```typescript
const isAllowedFetchImageTarget = (value: string): boolean => {
    try {
        const url = new URL(value);
        return /^https?:$/i.test(url.protocol) && !isPrivateHostname(url.hostname);
    } catch { return false; }
};

const handleFetchImageProxyRequest = async (req, res, next, logger) => {
    // ... OPTIONS 处理
    const target = String(new URL(req.url, 'http://local-fetch-image').searchParams.get('url') || '').trim();
    if (!target || !isAllowedFetchImageTarget(target)) {
        res.statusCode = 400; return;
    }
    const upstream = await fetch(target, { headers: { Accept: 'image/*,*/*;q=0.8' } });
    if (!upstream.ok) { res.statusCode = upstream.status; return; }
    const buf = Buffer.from(await upstream.arrayBuffer());
    res.statusCode = 200;
    res.setHeader('Content-Type', upstream.headers.get('content-type') || 'application/octet-stream');
    res.end(buf);
};
```

#### 7.1.3 注册中间件

```typescript
// configureServer 和 configurePreviewServer 中：
server.middlewares.use('/api/image-backend/fetch-image', async (req, res, next) => {
    await handleFetchImageProxyRequest(req, res, next, server.config.logger);
});
```

**冲突**：⚠️ 低。MCX 没有修改 `vite.config.ts`。

---

## 第8步：UI 层

### 8.1 `ImageGenerationSettings.tsx`

新增"经代理访问"开关 + 自定义代理地址输入框：

```tsx
<div className="...">
    <div>
        <div className="text-sm font-bold text-fuchsia-200">经代理访问</div>
        <div className="text-xs text-gray-400">开启后生图请求走代理，解决 CORS 跨域问题。</div>
    </div>
    <ToggleSwitch
        checked={Boolean(form.功能模型占位.图片需要代理)}
        onChange={(next) => updatePlaceholder('图片需要代理', next)}
    />
</div>

<div className="...">
    <div>
        <div className="text-sm font-bold text-fuchsia-200">自定义图片代理地址</div>
        <div className="text-xs text-gray-400">可选。留空则使用默认代理。</div>
    </div>
    <input
        type="text"
        value={form.功能模型占位.自定义图片代理地址 || ''}
        onChange={(e) => updatePlaceholder('自定义图片代理地址', e.target.value)}
        placeholder="留空则使用默认代理（msjh.bacon159.pp.ua）"
        disabled={!form.功能模型占位.图片需要代理}
    />
</div>

{/* NSFW 区域同样新增 */}
<div className="...">
    <label className="text-sm font-bold text-rose-200">NSFW 自定义图片代理地址</label>
    <input
        type="text"
        value={form.功能模型占位.NSFW自定义图片代理地址 || ''}
        onChange={(e) => updatePlaceholder('NSFW自定义图片代理地址', e.target.value)}
        disabled={!form.功能模型占位.NSFW生图独立接口启用}
    />
</div>
```

**冲突**：✅ 无

### 8.2 立绘显示组件

在以下组件中增加 `立绘图片URL` fallback：

| 组件 | 改动 |
|------|------|
| `MobileCharacter.tsx` | `\|\| 获取图片资源文本地址(character?.立绘图片URL)` |
| `EquipmentModal.tsx` | `\|\| 获取图片资源文本地址(character?.立绘图片URL)` |
| `SocialModal.tsx` | `return fromArchive \|\| 获取图片资源文本地址(npc?.立绘图片URL)` |
| `MobileSocial.tsx` | 同上 |
| `TeamModal.tsx` | `\|\| 获取图片资源文本地址(person?.立绘图片URL)` |
| `MobileTeamModal.tsx` | 同上 |

**冲突**：✅ 无

---

## 冲突风险汇总

| 风险 | 文件 | 说明 |
|------|------|------|
| ⚠️ 中 | `imageTasks.ts` | MCX 已修改部分内容，需确认不重复 |
| ⚠️ 中 | `dbService.ts` | MCX 已修改图床/配额部分，VERSION 可能冲突 |
| ⚠️ 中 | `apiConfig.ts` | MCX 已添加货币字段，需合并 |
| ⚠️ 中 | `imageGenerationDiagnostics.ts` | MCX 已修改 import |
| ✅ 低 | 其他文件 | 纯新增或增量 |

---

## 验证清单

- [ ] 旧存档（无 systems 字段）走基础生图逻辑，无回归
- [ ] localhost 自动走 Vite 代理
- [ ] 物品生图中文模式正常
- [ ] 图库复用拦截生效
- [ ] 立绘 URL fallback 正常
- [ ] 所有测试通过
