# 货币系统移植分析

## 移植策略概览

| 类型 | 文件 | 策略 |
|------|------|------|
| 新增文件 | 4 个 | 直接复制 |
| 低风险修改 | 3 个 | cherry-pick 或手动合并 |
| 高风险修改 | 4 个 | 需要手动 merge |

---

## 一、新增文件（直接复制）

以下文件在 main 分支不存在，可直接从 Rebase 复制：

| 文件 | 行数 | 说明 |
|------|------|------|
| `utils/currencyTemplateExpander.ts` | 75 | 品质模板展开、批量转化展开 |
| `utils/currencySystemLoader.ts` | 139 | 货币系统加载、汇率校验 |
| `utils/economyRules.ts` | 213 | 汇率时效管理、汇率检查、汇率生成提示 |
| `prompts/runtime/currencyReference.ts` | 143 | 货币参考提示词构建 |
| `__tests__/currencyReference.test.ts` | 215 | 测试 |
| `__tests__/currencySystemLoader.test.ts` | 55 | 测试 |
| `__tests__/economyRules.test.ts` | 38 | 测试 |
| `__tests__/exchangeRateIntegration.test.ts` | 252 | 测试 |

**操作**：`git checkout Rebase -- <file>`

---

## 二、低风险修改（cherry-pick）

### 2.1 `models/world.ts`

**改动**：地图层级结构新增 4 个汇率字段 + 世界数据结构新增 `_汇率生成任务`

**风险评估**：main 也有货币相关修改，但主要是灵石显示修复，不涉及 `地图层级结构` 字段新增。

**操作**：尝试 cherry-pick `7e261d33`（实现多货币体系地点汇率自动生成机制）

### 2.2 `services/auctionHouse.ts`

**改动**：`计算物品市场BaseAmount` 返回 `number | null` + 手续费提示

**风险评估**：main 有 `货币去重` 修改，可能在同一文件但不冲突。

**操作**：cherry-pick `18f48044`（拍卖手续费文案改为按 feeRate 计算）

### 2.3 `utils/currencyDisplay.ts`

**改动**：适配多货币系统显示

**风险评估**：main 有 `CurrencySystem 函数名中文化` 修改，可能冲突。

**操作**：需要手动 merge

---

## 三、高风险修改（手动 merge）

### 3.1 `models/system.ts`

**Rebase 改动**：
- 新增 8 个货币相关接口（`QualityTemplate`、`UnitDefinition`、`TransformDefinition` 等）
- `功能模型占位配置结构` 新增代理字段
- `初始伙伴配置结构` 新增 `立绘图片URL`
- 新增图片供应商配置

**main 改动**：
- `CurrencySystem 函数名中文化`（`7d1420de`）
- 其他货币相关修复

**冲突风险**：高。main 和 Rebase 都大量修改了 `models/system.ts`。

**操作**：手动合并，以 main 为基础，逐个添加 Rebase 的新增接口。

### 3.2 `utils/apiConfig.ts`

**Rebase 改动**：
- 新增 `是否多货币模式()`、`获取展开货币系统()`、`重置货币系统缓存()`
- `获取文生图接口配置` 透传代理字段
- 新增 `openai_custom` 供应商类型

**main 改动**：
- 货币汇率相关修复

**冲突风险**：中。新增函数不冲突，但上下文可能有变化。

### 3.3 `hooks/useGame/systemPromptBuilder.ts`

**Rebase 改动**：
- 多货币快照构建
- 货币参考注入 + 汇率快照注入
- 叙事约束天赋支持
- 删除世界事件分档

**main 改动**：
- 未知（main 有 21 个新提交）

**冲突风险**：高。

### 3.4 `hooks/useGame/responseCommandProcessor.ts`

**Rebase 改动**：
- 新增汇率检查（地点变更时触发）
- NPC 在场判定简化
- 记忆系统传递

**main 改动**：
- 未知

**冲突风险**：高。

### 3.5 `hooks/useGame/stateTransforms.ts`

**Rebase 改动**：
- 新增 `迁移货币桶旧单位名()`
- 天赋叙事约束字段
- 删除功法合并

**main 改动**：
- 未知

**冲突风险**：高。

### 3.6 `hooks/useGame.ts`

**Rebase 改动**：
- 适配命令处理器新接口
- 新增记忆系统传递
- 新增 `获取运行时配置` 依赖注入

**main 改动**：
- 未知

**冲突风险**：高。

---

## 四、建议移植顺序

1. **新增文件**（无冲突）：直接 `git checkout Rebase -- <file>`
2. **models/world.ts**：cherry-pick 或手动添加字段
3. **services/auctionHouse.ts**：cherry-pick
4. **utils/apiConfig.ts**：手动合并新增函数
5. **models/system.ts**：手动合并新增接口
6. **utils/currencyDisplay.ts**：手动合并
7. **hooks/useGame/stateTransforms.ts**：手动合并
8. **hooks/useGame/responseCommandProcessor.ts**：手动合并
9. **hooks/useGame/systemPromptBuilder.ts**：手动合并
10. **hooks/useGame.ts**：手动合并

---

## 五、main 分支已有但 Rebase 没有的货币功能

main 分支有但 Rebase 没有的修改（移植时可能需要保留）：
- `货币去重`（`e77bd19f`）
- `CurrencySystem 函数名中文化`（`7d1420de`）
- `灵石显示修复`（`6b21d3e0`）
- `legacy模式下禁用货币编辑器`（`ff992d55`）
- `动态货币系统修复`（`6d91d2d5`）
